[package]
name = "rust_webgl_compatibility_layer"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[lib]
name = "webgl"
path = "src/webgl_native.rs"
crate-type = ["cdylib"]

[features]
bundled = []

[profile.release]
strip = true
lto = true
codegen-units = 1
